export enum SslMode {
  DISABLE = 'DISABLE',
  ALLOW = 'ALLOW',
  PREFER = 'PREFER',
  REQUIRE = 'REQUIRE',
  VERIFY_CA = 'VERIFY_CA',
  VERIFY_FULL = 'VERIFY_FULL',
}

export enum DbType {
  POSTGIS = 'postgis',
}
